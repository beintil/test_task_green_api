{
  "server": {
    "port": 8080,
    "read_timeout": "10s",
    "write_timeout": "10s",
    "max_header_bytes": 1000000
  },
  "handler": {
    "request_timeout": "10s",
    "allowed_cors_origins": "https://yourdomain.com"
  }
}
